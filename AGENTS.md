# Project instructions

- Address the user as Mr. X.
- Do not use emojis.
- Use ASCII hyphens instead of em dashes.
- Use `uv` for Python commands and dependency management.
- For nontrivial changes, inspect the codebase, clarify requirements, and persist a plan before editing.
